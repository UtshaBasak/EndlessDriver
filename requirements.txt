# EndlessDriver dependencies
# Install with:  pip install -r requirements.txt

# Core rendering library. Provides OpenGL, GLU and GLUT bindings.
PyOpenGL>=3.1.0

# Optional C-accelerated routines. Improves frame rate, but needs a working
# compiler on some platforms. Uncomment if your system can build it.
# PyOpenGL-accelerate>=3.1.0
